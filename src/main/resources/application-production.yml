spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url:  jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_DATABASE:db}?serverTimezone=UTC&useUnicode=true&characterEncoding=utf-8&userSSL=false